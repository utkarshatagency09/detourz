{% if heading_title is not empty %}
<h3 class="module-title">{{ heading_title }}</h3>
{% endif %}
<div class="mz-tags">
  {% for tag in tags %}
    <a href="{{ tag.href }}" title="{{ tag.name }}" class="btn mb-1 btn-{{ tag_color }} btn-sm">{{ tag.name }}</a>
  {% endfor %}
</div>
